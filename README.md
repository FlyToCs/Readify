# 📖 پروژه فروشگاه کتاب با ASP.NET Core MVC

این پروژه یک وب‌سایت فروشگاه کتاب است که با استفاده از ASP.NET Core MVC پیاده‌سازی شده است. این پروژه قابلیت‌های اساسی یک فروشگاه آنلاین، شامل نمایش محصولات و یک پنل مدیریت کامل را پیاده‌سازی می‌کند.

## ✨ قابلیت‌های کلیدی

### بخش عمومی و کاربران (Front-End)

* **صفحه اصلی پویا:** طراحی صفحه اصلی فروشگاه.
* **نمایش دسته‌بندی‌ها:** لیست دسته‌بندی‌های کتاب از دیتابیس لود شده و نمایش داده می‌شود.
* **نمایش جدیدترین کتاب‌ها:** ۵ مورد از جدیدترین کتاب‌های ثبت شده در دیتابیس در صفحه اصلی نمایش داده می‌شوند.
* **سیستم ثبت نام و ورود:** کاربران می‌توانند در سایت ثبت نام کرده و وارد شوند.
* **انتخاب نقش:** کاربران هنگام ثبت نام می‌توانند نقش خود را (ادمین یا مشتری) انتخاب کنند.
* **مدیریت تصاویر:** تصاویر مربوط به کتاب‌ها و دسته‌بندی‌ها در پوشه `wwwroot` ذخیره شده و آدرس آن‌ها در دیتابیس نگهداری می‌شود.
* **طراحی واکنش‌گرا و فونت فارسی:** استفاده از فونت فارسی مناسب (مانند Vazir یا IRANSans) برای خوانایی بهتر.

### بخش مدیریت (Admin Panel)

* **دسترسی ادمین:** کاربرانی که با نقش "ادمین" وارد می‌شوند، به صفحه مدیریت هدایت می‌شوند.
* **مدیریت کاربران (CRUD):** ادمین قابلیت ثبت، نمایش، ویرایش و حذف کاربران را دارد.
* **مدیریت دسته‌بندی‌ها (CRUD):** ادمین قابلیت ثبت، نمایش، ویرایش و حذف دسته‌بندی‌های کتاب را دارد.
* **مدیریت کتاب ها (CRUD):** ادمین قابلیت ثبت، نمایش، ویرایش و حذف کتاب را دارد.

---

## 🛠️ تکنولوژی‌های استفاده شده

* **ASP.NET Core MVC:** برای ساختار اصلی وب اپلیکیشن.
* **Entity Framework Core:** برای کار با دیتابیس (ORM).
* **ASP.NET Core Identity:** برای مدیریت کاربران، نقش‌ها، ثبت نام و ورود.
* **SQL Server (یا SQLite):** به عنوان پایگاه داده.
* **Razor Views:** برای طراحی صفحات وب.
* **HTML / CSS / Bootstrap:** برای طراحی ظاهری و واکنش‌گرایی.

---

## 🚀 راه‌اندازی و اجرا

برای اجرای این پروژه در سیستم خود، مراحل زیر را دنبال کنید:

### ۱. پیش‌نیازها

* [.NET 6.0 SDK](https://dotnet.microsoft.com/en-us/download/dotnet/6.0) (یا نسخه جدیدتر)
* [SQL Server](https://www.microsoft.com/en-us/sql-server/sql-server-downloads) (یا دیتابیس دیگری که با EF Core سازگار است)
* [Git](https://git-scm.com/downloads)

### ۲. مراحل نصب

۱. **کلون کردن ریپازیتوری:**
   ```bash
   git clone https://github.com/FlyToCs/Readify.git
